<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.foureyes.moai.backend.domain.user.mapper.UserMapper">

    <insert id="insertUser" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (email, password, name, provider, provider_id, profile_image_url, is_verified, is_deleted)
        VALUES (#{email}, #{password}, #{name}, #{provider}, #{providerId}, #{profileImageUrl}, #{isVerified}, #{isDeleted})
    </insert>

    <select id="findByEmail" resultType="User">
        SELECT * FROM users WHERE email = #{email}
    </select>
</mapper>
